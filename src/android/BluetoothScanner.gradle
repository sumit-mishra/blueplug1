repositories {
jcenter()
 flatDir {
      dirs 'libs'
   }
}
dependencies {
  compile(name : 'Rfid.AsciiProtocol-1.4.5-release',ext:'aar') 
}
  
  android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
  }
  
